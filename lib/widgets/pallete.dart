import 'package:flutter/material.dart';

class Pallete {
  // Modern primary colors
  static const Color primaryDark = Color(0xFF0A2342);  // Deep navy
  static const Color primary = Color(0xFF2C4E7F);      // Medium blue
  static const Color primaryLight = Color(0xFF4C7FBF);  // Soft blue

  // Modern accent colors
  static const Color accent = Color(0xFF4ECDC4);       // Teal
  static const Color accentLight = Color(0xFFA7E8DF);  // Light teal
  static const Color highlight = Color(0xFFFF6B6B);    // Coral

  // Text colors
  static const Color mainFontColor = primaryDark;
  static const Color secondaryFontColor = Color(0xFF5F6C7B);

  // Suggestion boxes (now with better hierarchy)
  static const Color firstSuggestionBoxColor = Color(0xFFE6F7FF);  // Lightest blue
  static const Color secondSuggestionBoxColor = Color(0xFFD0E8F7); // Medium blue
  static const Color thirdSuggestionBoxColor = Color(0xFFB2D8EC);  // Darker blue

  // UI Elements
  static const Color assistantCircleColor = Color(0xFFE1F5FE);
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color canvasColor = Color(0xFFF8F9FA);

  // History bubbles
  static const Color historyUserColor = Color(0xFFE3F2FD);  // Light blue
  static const Color historyAiColor = Color(0xFFFFF3E0);    // Warm white

  // Standard colors
  static const Color blackColor = Color(0xFF2D3436);
  static const Color whiteColor = Color(0xFFFFFFFF);
  static const Color errorColor = Color(0xFFFF4D4D);
}